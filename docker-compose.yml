version: '2'
services:
  userapp:
    build: userapp
    ports:
    - "3000:3000"
  controllersrv:
    build: controllersrv
    ports:
    - "3001:3001"
  listsrv:
    build: listsrv
  modsrv:
    build: modsrv
  rethinkdb:
    image: "rethinkdb:latest"
    volumes:
    - /data
